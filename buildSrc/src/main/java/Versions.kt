object Versions {
    const val rxAnimation = "1.0.0"
    const val biometric = "1.1.0"
    const val kotlinVersion = "1.7.10"
    const val androidPluginVersion = "4.1.2"
    const val dexCountVersion = "0.8.2"
    const val googlePlayVersion = "4.3.8"
    const val crashlyticsGradlePluginVersion = "2.5.0"
    const val navigationPluginVersion = "2.3.3"

// Compile dependencies

    // Support
    const val appCompatVersion = "1.5.0"
    const val cardViewVersion = "1.0.0"
    const val recyclerViewVersion = "1.2.1"
    const val constraintLayoutVersion = "2.1.4"
    const val materialDesignVersion = "1.6.1"
    const val preferenceManagerVersion = "1.1.1"
    const val coordinatorLayoutVersion = "1.2.0"
    const val activityVersion = "1.5.1"
    const val fragmentVersion = "1.5.2"
    const val firebaseBoMVersion = "28.1.0"
    const val emojiSupportVersion = "1.2.0"

    // Architecture Components
    const val archCompVersion = "2.5.1"
    const val roomVersion = "2.4.3"

    // Security Crypto
    const val securityCryptoVersion = "1.0.0"

    // Sqlite encryption
    const val sqlChipherVersion = "4.4.3"
    const val sqliteVersion = "2.1.0"

    // Navigation Components
    const val navigationVersion = "2.3.5"

    // Core-KTX
    const val coreKtxVersion = "1.6.0-rc01"

    // Google Maps
    const val playServicesMapsVersion = "17.0.1"
    const val playServicesLocationVersion = "18.0.0"
    const val playServicesPlacesVersion = "17.0.0"
    const val playServicesAuthVersion = "19.0.0"

    // Timber
    const val timberVersion = "4.7.1"

    // Retrofit
    const val retrofitVersion = "2.9.0"
    const val retrofitScalarVersion = "2.9.0"

    // OkHttp
    const val okhttpVersion = "4.9.1"
    const val okhttpLoggingVersion = okhttpVersion

    // RxJava
    const val rxJava2Version = "2.2.21"

    // RxKotlin
    const val rxKotlinVersion = "2.4.0"

    // RxAndroid
    const val rxAndroidVersion = "2.1.1"

    // RxBinding
    const val rxBindingXVersion = "3.0.0-alpha2"

    // RxMath
    const val rxMathVersion = "0.20.0"

    // Dagger
    const val daggerVersion = "2.20"

    // MultiDex
    const val multiDexVersion = "1.0.1"

    // Glide
    const val glideVersion = "4.12.0"

    // GlassFish
    const val glassFishVersion = "10.0-b28"

    // Epoxy
    const val epoxyVersion = "2.19.0"

    // CircleImageView
    const val circleImageViewVersion = "3.1.0"

    // SnapHelper
    const val snapHelperVersion = "1.5"

    // RxPermissions
    const val runtimePermissionVersion = "0.10.2"

    // Stetho
    const val stethoVersion = "1.6.0"

    // Test dependency versions
    const val androidXJunitVersion = "1.1.2"
    const val espressoVersion = "3.3.0"
    const val gsonVersion = "2.8.9"
    const val junitVersion = "4.13.2"
    const val mockitoVersion = "3.11.0"
    const val testRunnerVersion = "1.3.0"
    const val testRulesVersion = "1.3.0"
    const val archCompTestVersion = "2.1.0"

    // Facebook SDK Version
    const val facebookSdkVersion = "13.2.0"
    const val facebookShimmerVersion = "0.5.0"

    // ViewPager2
    const val viewPager2Version = "1.0.0"

    // Firebase Crashlytics
    const val crashlyticsVersion = "18.0.1"
    const val firebaseAnalyticsVersion = "19.0.0"
    const val firebaseMessagingVersion = "22.0.0"

    // Ucrop
    const val uCropVersion = "2.2.7"

    // Photo View
    const val photoViewVersion = "2.3.0"
    const val touchImageViewVersion = "3.1.0"

    // libphonenumber
    const val libphonenumberVersion = "8.12.7"

    const val countryCodeVersion = "2.5.3"

    const val stepsIndicatorVersion = "0.0.3"

    // Stripe
    const val stripeVersion = "16.3.0"

    // Paging3
    const val pagingVersion = "3.1.0-alpha01"

    // TimeAgo
    const val timeAgoVersion = "4.0.3"

    // Matisse
    const val matisseVersion = "0.5.3-beta3"

    // Swipe Refresh Layout
    const val swipeRefreshLayout = "1.1.0"

    const val barcodeScanner = "16.2.0"
    const val camera = "1.1.0-alpha01"
    const val cameraView = "1.0.0-alpha21"

    const val signalR = "7.0.0-preview.4.22251.1"
    const val emlPaymentsVersion = "3.1.0"
    const val lottieVersion = "5.2.0"
    const val dotLottieVersion = "1.1"

    // SMS retriever
    const val smsRetrieverApiVersion = "18.0.1"
    const val smsRetrieverVersion = "20.3.0"

    // ONE SIGNAL
    const val oneSignalVersion = "[4.0.0, 4.99.99]"
}
